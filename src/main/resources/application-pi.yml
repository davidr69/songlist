---
spring:
  jpa:
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        ddl-auto: update
        # Hibernate ddl auto (create, create-drop, validate, update)
        jdbc:
          batch_size: 100
  datasource:
    url: jdbc:postgresql://dev-db.lavacro.net:5432/songs?reWriteBatchedInserts=true
